<%= simple_form_for(@task) do |f| %>

<%= f.input :name, placeholder: "to do"     %>
<%= f.input :content,  placeholder: "the detail" %>
Completed?
<%= f.check_box :completed, class: "fa fa-checkbox"  %>

<br>
<br>

<%= f.button :submit, class: "btn btn-primary btn-md" %>

<% end %>